---
import type { HTMLTag, Polymorphic } from "astro/types";

type Props<Tag extends HTMLTag> = Polymorphic<{ as: Tag }>;
const { as: Tag = "section" } = Astro.props;
---

<Tag class="px-4 sm:px-7 md:px-10 xl:px-20 max-w-screen-xl mx-auto">
  <slot />
</Tag>
